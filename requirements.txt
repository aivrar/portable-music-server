# Core requirements for Music Module
# All installed into embedded Python by install.bat
# (Model dependencies are installed in separate venvs)

# --- GUI & Utilities ---
virtualenv>=20.24.0
huggingface_hub>=0.20.0
requests>=2.28.0
tqdm>=4.64.0

# --- API Server ---
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
httpx>=0.25.0

# --- Audio Processing Pipeline ---
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0
pydub>=0.25.0
noisereduce>=2.0.0
pyloudnorm>=0.1.0

# pyrubberband requires the rubberband CLI binary (optional, tempo adjust
# will gracefully skip if unavailable)
pyrubberband>=0.3.0
